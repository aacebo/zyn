# Testing

zyn provides two categories of testing tools:

- **[Assertions](./assertions.md)** — macros for comparing token streams, checking substrings, and inspecting diagnostics produced by elements
- **[Debugging](./debugging.md)** — inspect generated code at compile time with `debug` / `debug = "pretty"` and the `ZYN_DEBUG` environment variable
